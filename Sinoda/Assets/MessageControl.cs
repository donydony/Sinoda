using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MessageControl : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void changeMessage(int player, int by_point)
    {
        //TextMesh t = GameObject.Find("Message").GetComponent<TextMesh>();
        //t.text="Player " + player + " won by " + by_point;
    }
}
